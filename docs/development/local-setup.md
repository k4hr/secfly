# Локальная подготовка

Требуются Node.js 22, pnpm и Docker Compose. Выполните `corepack enable`, затем `pnpm install --frozen-lockfile`. Скопируйте безопасные значения из `.env.example` в локальный `.env`, если требуется изменить порты. Реальные устройства и секреты не нужны и не поддерживаются.
